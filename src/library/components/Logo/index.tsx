import { FC } from 'react';

import st from './index.module.scss';

const Logo: FC = () => {
  return (
    <figure className={st.logo}>
      <img src='#' />
    </figure>
  );
};

export default Logo;
